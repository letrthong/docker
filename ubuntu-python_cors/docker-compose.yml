version: '3.8'

services:
  web_service:
    container_name: telua_python_web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./config:/app/config
      - ./key:/app/key
    stdin_open: true
    tty: true
