version: '3.8'

services:
  web_service:
    container_name: telua_python_point
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
      - "5173:5173"
    volumes:
      - ./:/app/
    stdin_open: true
    tty: true
